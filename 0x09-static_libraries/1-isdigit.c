#include "main.h"

/**
 * _isdigit - for checking if a character is digit
 * @c: chacter to be checked
 * Return: 1 or 0 based on the condition
 */

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
