#include "main.h"
#include <stdio.h>

/**
* main - for printing its name
*@argc: the argument count
*@argv: argument variable
*Return: 0
*/
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
